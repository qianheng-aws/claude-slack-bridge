#!/bin/bash
# Wrapper that activates the venv before running the hook CLI
PROJECT_DIR="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
exec "$PROJECT_DIR/.venv/bin/claude-slack-bridge" hook "$@"
